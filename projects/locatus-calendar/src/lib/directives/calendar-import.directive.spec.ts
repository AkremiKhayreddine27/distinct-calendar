import { CalendarImportDirective } from './calendar-import.directive';

describe('CalendarImportDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarImportDirective();
    expect(directive).toBeTruthy();
  });
});
